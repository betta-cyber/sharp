-
  url: $requests
  data-format: "json"
  data:
    url: "https://api.github.com/repos/grpc/grpc/releases"
  source: "grpc"
  pattern:
    type: "list"
    selector: "data"
  response:
    url:
      type: key
      struct: string
      pattern: "url"
    component:
      type: static
      struct: string
      pattern: 'grpc'
    commit_time:
      type: key
      struct: string
      pattern: 'published_at'
    description:
      type: key
      struct: string
      pattern: 'body'
    version:
      type: key
      struct: string
      pattern: 'tag_name'
    source:
      type: key
      struct: string
      pattern: "url"
    source_platform:
      type: static
      struct: string
      pattern: '来源：Github'
    commit_user:
      type: static
      struct: string
      pattern: 'sharp'
    level:
      type: static
      struct: string
      pattern: '未知'
    cve_id:
      type: func
      struct: string
      pattern: 'find_cve'
    update_type:
      type: func
      struct: string
      pattern: 'check_update_type'
    update_title:
      type: key
      struct: string
      pattern: 'name'
-
  url: $requests
  data-format: "json"
  data:
    url: "https://api.github.com/repos/mybatis/mybatis-3/releases"
  source: "mybatis"
  pattern:
    type: "list"
    selector: "data"
  response:
    url:
      type: key
      struct: string
      pattern: "url"
    component:
      type: static
      struct: string
      pattern: 'mybatis'
    commit_time:
      type: key
      struct: string
      pattern: 'published_at'
    description:
      type: key
      struct: string
      pattern: 'body'
    version:
      type: key
      struct: string
      pattern: 'tag_name'
    source:
      type: key
      struct: string
      pattern: "url"
    source_platform:
      type: static
      struct: string
      pattern: '来源：Github'
    commit_user:
      type: static
      struct: string
      pattern: 'sharp'
    level:
      type: static
      struct: string
      pattern: '未知'
    cve_id:
      type: func
      struct: string
      pattern: 'find_cve'
    update_type:
      type: func
      struct: string
      pattern: 'check_update_type'
    update_title:
      type: key
      struct: string
      pattern: 'name'
-
  url: $requests
  data-format: "json"
  data:
    url: "https://api.github.com/repos/baomidou/mybatis-plus/releases"
  source: "mybatis-plus"
  pattern:
    type: "list"
    selector: "data"
  response:
    url:
      type: key
      struct: string
      pattern: "url"
    component:
      type: static
      struct: string
      pattern: 'mybatis-plus'
    commit_time:
      type: key
      struct: string
      pattern: 'published_at'
    description:
      type: key
      struct: string
      pattern: 'body'
    version:
      type: key
      struct: string
      pattern: 'tag_name'
    source:
      type: key
      struct: string
      pattern: "url"
    source_platform:
      type: static
      struct: string
      pattern: '来源：Github'
    commit_user:
      type: static
      struct: string
      pattern: 'sharp'
    level:
      type: static
      struct: string
      pattern: '未知'
    cve_id:
      type: func
      struct: string
      pattern: 'find_cve'
    update_type:
      type: func
      struct: string
      pattern: 'check_update_type'
    update_title:
      type: key
      struct: string
      pattern: 'name'
-
  url: $requests
  data-format: "json"
  data:
    url: "https://api.github.com/repos/xetorthio/jedis/releases"
  source: "jedis"
  pattern:
    type: "list"
    selector: "data"
  response:
    url:
      type: key
      struct: string
      pattern: "url"
    component:
      type: static
      struct: string
      pattern: 'jedis'
    commit_time:
      type: key
      struct: string
      pattern: 'published_at'
    description:
      type: key
      struct: string
      pattern: 'body'
    version:
      type: key
      struct: string
      pattern: 'tag_name'
    source:
      type: key
      struct: string
      pattern: "url"
    source_platform:
      type: static
      struct: string
      pattern: '来源：Github'
    commit_user:
      type: static
      struct: string
      pattern: 'sharp'
    level:
      type: static
      struct: string
      pattern: '未知'
    cve_id:
      type: func
      struct: string
      pattern: 'find_cve'
    update_type:
      type: func
      struct: string
      pattern: 'check_update_type'
    update_title:
      type: key
      struct: string
      pattern: 'name'
-
  url: $requests
  data-format: "json"
  data:
    url: "https://api.github.com/repos/redisson/redisson/releases"
  source: "redisson"
  pattern:
    type: "list"
    selector: "data"
  response:
    url:
      type: key
      struct: string
      pattern: "url"
    component:
      type: static
      struct: string
      pattern: 'redisson'
    commit_time:
      type: key
      struct: string
      pattern: 'published_at'
    description:
      type: key
      struct: string
      pattern: 'body'
    version:
      type: key
      struct: string
      pattern: 'tag_name'
    source:
      type: key
      struct: string
      pattern: "url"
    source_platform:
      type: static
      struct: string
      pattern: '来源：Github'
    commit_user:
      type: static
      struct: string
      pattern: 'sharp'
    level:
      type: static
      struct: string
      pattern: '未知'
    cve_id:
      type: func
      struct: string
      pattern: 'find_cve'
    update_type:
      type: func
      struct: string
      pattern: 'check_update_type'
    update_title:
      type: key
      struct: string
      pattern: 'name'
-
  url: $requests
  data-format: "json"
  data:
    url: "https://api.github.com/repos/lettuce-io/lettuce-core/releases"
  source: "lettuce"
  pattern:
    type: "list"
    selector: "data"
  response:
    url:
      type: key
      struct: string
      pattern: "url"
    component:
      type: static
      struct: string
      pattern: 'lettuce'
    commit_time:
      type: key
      struct: string
      pattern: 'published_at'
    description:
      type: key
      struct: string
      pattern: 'body'
    version:
      type: key
      struct: string
      pattern: 'tag_name'
    source:
      type: key
      struct: string
      pattern: "url"
    source_platform:
      type: static
      struct: string
      pattern: '来源：Github'
    commit_user:
      type: static
      struct: string
      pattern: 'sharp'
    level:
      type: static
      struct: string
      pattern: '未知'
    cve_id:
      type: func
      struct: string
      pattern: 'find_cve'
    update_type:
      type: func
      struct: string
      pattern: 'check_update_type'
    update_title:
      type: key
      struct: string
      pattern: 'name'
-
  url: $requests
  data-format: "json"
  data:
    url: "https://api.github.com/repos/apache/dubbo/releases"
  source: "dubbo"
  pattern:
    type: "list"
    selector: "data"
  response:
    url:
      type: key
      struct: string
      pattern: "url"
    component:
      type: static
      struct: string
      pattern: 'dubbo'
    commit_time:
      type: key
      struct: string
      pattern: 'published_at'
    description:
      type: key
      struct: string
      pattern: 'body'
    version:
      type: key
      struct: string
      pattern: 'tag_name'
    source:
      type: key
      struct: string
      pattern: "url"
    source_platform:
      type: static
      struct: string
      pattern: '来源：Github'
    commit_user:
      type: static
      struct: string
      pattern: 'sharp'
    level:
      type: static
      struct: string
      pattern: '未知'
    cve_id:
      type: func
      struct: string
      pattern: 'find_cve'
    update_type:
      type: func
      struct: string
      pattern: 'check_update_type'
    update_title:
      type: key
      struct: string
      pattern: 'name'
-
  url: $requests
  data-format: "json"
  data:
    url: "https://api.github.com/repos/apache/rocketmq/releases"
  source: "rocketmq"
  pattern:
    type: "list"
    selector: "data"
  response:
    url:
      type: key
      struct: string
      pattern: "url"
    component:
      type: static
      struct: string
      pattern: 'rocketmq'
    commit_time:
      type: key
      struct: string
      pattern: 'published_at'
    description:
      type: key
      struct: string
      pattern: 'body'
    version:
      type: key
      struct: string
      pattern: 'tag_name'
    source:
      type: key
      struct: string
      pattern: "url"
    source_platform:
      type: static
      struct: string
      pattern: '来源：Github'
    commit_user:
      type: static
      struct: string
      pattern: 'sharp'
    level:
      type: static
      struct: string
      pattern: '未知'
    cve_id:
      type: func
      struct: string
      pattern: 'find_cve'
    update_type:
      type: func
      struct: string
      pattern: 'check_update_type'
    update_title:
      type: key
      struct: string
      pattern: 'name'
-
  url: $requests
  data-format: "json"
  data:
    url: "https://api.github.com/repos/apache/kafka/releases"
  source: "kafka"
  pattern:
    type: "list"
    selector: "data"
  response:
    url:
      type: key
      struct: string
      pattern: "url"
    component:
      type: static
      struct: string
      pattern: 'kafka'
    commit_time:
      type: key
      struct: string
      pattern: 'published_at'
    description:
      type: key
      struct: string
      pattern: 'body'
    version:
      type: key
      struct: string
      pattern: 'tag_name'
    source:
      type: key
      struct: string
      pattern: "url"
    source_platform:
      type: static
      struct: string
      pattern: '来源：Github'
    commit_user:
      type: static
      struct: string
      pattern: 'sharp'
    level:
      type: static
      struct: string
      pattern: '未知'
    cve_id:
      type: func
      struct: string
      pattern: 'find_cve'
    update_type:
      type: func
      struct: string
      pattern: 'check_update_type'
    update_title:
      type: key
      struct: string
      pattern: 'name'
